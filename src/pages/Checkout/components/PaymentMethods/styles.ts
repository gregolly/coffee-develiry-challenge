import styled from 'styled-components'

export const PaymentMethodsContainer = styled.div`
  background-color: ${({ theme }) => theme.colors['base-card']};
  padding: 2.5rem;
  border-radius: 6px;

  margin-top: 0.75rem;
`
export const ButtonMethodsContainer = styled.div`
  display: flex;
  gap: 12px;

  margin-top: 2rem;
`
